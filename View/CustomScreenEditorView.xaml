<UserControl x:Class="AutomationStudio.View.CustomScreenEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AutomationStudio.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ToolBar Grid.Row="0">
            <Button Command="{Binding CmdAppend}">
                <Image Width="20" Height="20" Source="/Automation.PluginCore;component/Image/plus.png"/>
            </Button>
            <Button Command="{Binding CmdRemove}">
                <Image Width="20" Height="20" Source="/Automation.PluginCore;component/Image/remove.png"/>
            </Button>
            <Separator/>
            <Button Command="{Binding CmdSave}">
                <Image Width="20" Height="20" Source="/Automation.PluginCore;component/Image/save.png"/>
            </Button>
        </ToolBar>
        <ItemsControl ItemsSource="{Binding Items}" Grid.Row="1">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemContainerStyle>
                <Style TargetType="{x:Type ContentPresenter}">
                    <Setter Property="Canvas.Left" Value="{Binding X}" />
                    <Setter Property="Canvas.Top" Value="{Binding Y}" />
                </Style>
            </ItemsControl.ItemContainerStyle>
        </ItemsControl>
    </Grid>
</UserControl>
